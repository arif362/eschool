<div class="container">
  <%= form_for @user_assign_course do |f| %>
      <% if @user_assign_course.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@user_assign_course.errors.count, "error") %> prohibited this user from being saved:</h2>

            <ul>
              <% @user_assign_course.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
            </ul>
          </div>
      <% end %>
      <div class="form-group">
        <%= f.label :user_id %>
        <%= f.select :user_id, User::USER_NAME ,class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= f.submit class: 'btn btn-default' %>
      </div>
  <% end %>
</div>